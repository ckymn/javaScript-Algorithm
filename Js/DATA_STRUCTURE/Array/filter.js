/**
 * {filter}: Kontrolu saglayan tum ogelerle birlikte yeni bir dizi olusturur
 */

const words = ['spray', 'limit', 'elite', 'muhammet', 'cumhuriyet']
const result = words.filter(w => w.length > 6)
console.log(result)
